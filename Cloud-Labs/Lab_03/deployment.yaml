apiVersion: apps/v1
kind: Deployment
metadata:
  name: lab3-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lab3
  template:
    metadata:
      labels:
        app: lab3
    spec:
      containers:
      - name: app
        image: lab3-app:v1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5000